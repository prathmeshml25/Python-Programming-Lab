# 10. Write a Python program to combine two sets into one, containing all unique elements from both sets. Given Input: set_a = {1, 2, 3, 4} and set_b = {3, 4, 5, 6} 

set_a = {1, 2, 3, 4}
set_b = {3, 4, 5, 6}

result = set_a.union(set_b)

print(result)